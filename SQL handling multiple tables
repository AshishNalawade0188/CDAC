1)	create table 
cust_vehicle(sail_id int, 
custid int, 
vid int, 
sid int , 
buy_price int);

insert into 
cust_vehicle values(1,1,1,10,75000),
(2,1,2,10,79000),
(3,2,3,11,80000),
(4,3,3,11,75000),
(5,3,2,10,800000);

a)select cv.sail_id, cv.custid, c.cname, cv.buy_price 
from cust_vehicle cv, customer c;

b) select cv.sail_id, cv.custid, c.cname, cv.vid,v.vname,cv.sid,s.sname, cv.buy_price
from cust_vehicle cv,salesman s, vehicle v, customer c;
**********************************************************************************************************************************************

2)	select empno,ename,sal,job,emp.deptno,dname 
from emp,dept where (job = 'clerk' or job = 'salesman') and sal > 1200 and emp.deptno = dept.deptno;
**********************************************************************************************************************************************

3)	select ename,emp.deptno,dname,sal,comm 
from emp,dept 
where (emp.deptno=10 or emp.deptno= 30) and (comm=0 or comm is null);
**********************************************************************************************************************************************

4)	select e.ename,e.sal,s.grade
from emp e, Salgrade s,dept d 
where e.deptno = d.deptno and (sal between s.losal and s.hisal);
**********************************************************************************************************************************************

5)	select dept.deptno,dname,loc 
from emp, dept 
where job = 'clerk' and dept.deptno = emp.deptno;
