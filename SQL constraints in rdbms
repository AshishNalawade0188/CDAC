
1)	alter table vehicle 
add constraint x primary key(vid);

alter table customer 
add constraint y primary key(custid);

alter table salesman 
add constraint z primary key(sid);
***********************************************************************************************************

2)	alter table vehicle 
add column category varchar(15);

insert into vehicle(vid,vname,category,price,discount,milage,details) 
values(8,'passion','bike',90000,null,50,'for passionate rider'),
(9,'honda city','car',140000,null,20,'best honda world'),
(10,'tvs icube','electric scooter',140000,null,40,'best in new word');
***********************************************************************************************************

3)	create table dept11 
as select * from dept;

alter table dept11 
add constraint dept_p primary key(deptno);
***********************************************************************************************************

4)
***********************************************************************************************************
	                                                                                                                                                                                                        
5)	create table trainer( tid varchar(10) primary key,
tname varchar(20), 
cid varchar(10), 
salary float(7,2) default 20000, 
experience int check(experience>=10));


insert into trainer(tid,tname,cid,experience) 
values('t1','Amit','c2',11), 
('t2','Rajan','c1',13),
('t3','Shruti','c2',14),
('t4','Arti','c2',11),
('t5','Sameer','c3',10),
('t6','Sanjay',null,11),
('t7','Soha',null,15);

a)	update modules 
set cid = 'c8' 
where cname = 'Dot Net';

b)	update trainer 
set cid = 'c2'
where tname = 'Sanjay';

update trainer 
set cid = 'c15' 
where tname = 'Soha';
ERROR 1452 (23000): Cannot add or update a child row: a foreign key constraint fails (`knowitdb`.`trainer`, CONSTRAINT `a` FOREIGN KEY (`cid`) REFERENCES `modules` (`cid`))

c)	insert into trainer('t8','Miller','c17','wp',20);
ERROR 1064 (42000): You have an error in your SQL syntax; check the manual that corresponds to your MySQL server version for the right syntax to use near ''t8','Miller','c17','wp',20)' at line 1

d)	delete from modules 
where cname = 'Dig Data';

e)	mysql> delete 
from modules 
where cname = 'Linux';
ERROR 1451 (23000): Cannot delete or update a parent row: a foreign key constraint fails (`knowitdb`.`trainer`, CONSTRAINT `a` FOREIGN KEY (`cid`) REFERENCES `modules` (`cid`))

f)	drop table modules;
ERROR 3730 (HY000): Cannot drop table 'modules' referenced by a foreign key constraint 'a' on table 'trainer'.
