1) select deptno,sum(sal) 
from emp 
group by deptno;
+--------+----------+
| deptno | sum(sal) |
+--------+----------+
|     20 | 10875.00 |
|     30 |  9400.00 |
|     10 |  8750.00 |
+--------+----------+

*******************************************************************************************************************
2) elect deptno,sum(sal) 
from emp 
group by deptno 
having sum(sal) > 10000;
+--------+----------+
| deptno | sum(sal) |
+--------+----------+
|     20 | 10875.00 |
+--------+----------+
*******************************************************************************************************************
3) select sum(sal),deptno from emp where deptno != 30 group by deptno having sum(sal) > 5000;
+----------+--------+
| sum(sal) | deptno |
+----------+--------+
| 10875.00 |     20 |
|  8750.00 |     10 |
+----------+--------+
*******************************************************************************************************************
4)select max(sal) 
from emp 
where job = 'manager';
+----------+
| max(sal) |
+----------+
|  2975.00 |
+----------+
*******************************************************************************************************************

5) select job,round(avg(sal)),count(*) No_of_Emp
from emp 
group by job;
+-----------+-----------------+-----------+
| job       | round(avg(sal)) | No_of_Emp |
+-----------+-----------------+-----------+
| CLERK     |            1038 |         4 |
| SALESMAN  |            1400 |         4 |
| MANAGER   |            2758 |         3 |
| ANALYST   |            3000 |         2 |
| PRESIDENT |            5000 |         1 |
+-----------+-----------------+-----------+
******************************************************************************************************
6)select deptno,count(*)
from emp
group by deptno 
having count(*) > 4 ;
+--------+----------+
| deptno | count(*) |
+--------+----------+
|     20 |        5 |
|     30 |        6 |
+--------+----------+
********************************************************************************************************
7)select job,sum(sal),min(sal)
from emp 
where job = 'manager' ;
+---------+----------+----------+
| job     | sum(sal) | min(sal) |
+---------+----------+----------+
| MANAGER |  8275.00 |  2450.00 |
+---------+----------+----------+
******************************************************************************************************
8) select deptno,sum(sal)
from emp 
where job = 'clerk' 
group by deptno having count(*) >1 ;
+--------+----------+
| deptno | sum(sal) |
+--------+----------+
|     20 |  1900.00 |
+--------+----------+
************************************************************************************************
9)select deptno,count(*) 
from emp 
where sal > 1500 group by deptno;
+--------+----------+
| deptno | count(*) |
+--------+----------+
|     30 |        2 |
|     20 |        3 |
|     10 |        2 |
+--------+----------+
************************************************************************************************
10)select count(*),deptno 
from emp 
where job = 'analyst' 
group by deptno;
+----------+--------+
| count(*) | deptno |
+----------+--------+
|        2 |     20 |
+----------+--------+
************************************************************************************************
11) select count(*) 
from emp 
where comm is  not null and comm !=0;
+----------+
| count(*) |
+----------+
|        3 |
+----------+
************************************************************************************************
12)select min(sal) 
from emp 
where year(hiredate) = 1981;
+----------+
| min(sal) |
+----------+
|   950.00 |
+----------+
************************************************************************************************
13)select avg(sal)
  from emp
  where round(datediff(curdate(),hiredate)/365) >= 44;
+-------------+
| avg(sal)    |
+-------------+
| 2077.083333 |
+-------------+
************************************************************************************************
14) select count(*) 
from emp 
where job = 'clerk' and deptno = 10 or deptno = 30;
+----------+
| count(*) |
+----------+
|        7 |
+----------+
***********************************************************************************************
15)select count(*) 
from emp 
where job = 'salesman' and sal>1500;
+----------+
| count(*) |
+----------+
|        1 |
+----------+
***********************************************************************************************
16) select count(*) 
from emp 
where monthname(hiredate) = 'February';
+----------+
| count(*) |
+----------+
|        2 |
+----------+
***********************************************************************************************
17) select count(*) 
from emp 
group by year(hiredate) 
having count(*) > 4;
+----------+
| count(*) |
+----------+
|       10 |
+----------+
*************************************************************************************************
18)  select job,hiredate,sal,deptno 
from emp 
where job = 'clerk' and (deptno = 20 or deptno = 30 ) and monthname(hiredate) = 'December';
+-------+------------+--------+--------+
| job   | hiredate   | sal    | deptno |
+-------+------------+--------+--------+
| CLERK | 1980-12-17 | 800.00 |     20 |
| CLERK | 1981-12-03 | 950.00 |     30 |
+-------+------------+--------+--------+
*************************************************************************************************
19) select job,sal 
from emp 
where job =  'manager' and sal % 2 = 0;
+---------+---------+
| job     | sal     |
+---------+---------+
| MANAGER | 2850.00 |
| MANAGER | 2450.00 |
+---------+---------+
*************************************************************************************************
20)select year(hiredate),count(*) 
from emp 
group by year(hiredate);
+----------------+----------+
| year(hiredate) | count(*) |
+----------------+----------+
|           1980 |        1 |
|           1981 |       10 |
|           1982 |        2 |
|           1983 |        1 |
+----------------+----------+
